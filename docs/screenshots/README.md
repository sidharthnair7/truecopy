# Screenshots

Two of these are embedded in the project README:

| File | What it must show |
|---|---|
| `refusal.png` | Workspace after an audit: gate report with `DE REFUSED (2)`, and the source-vs-German diff with the missing tokens highlighted |
| `readback.png` | The "What a viewer sees" card with `es` selected, showing the Spanish title read back from YouTube |

The other three are for the Devpost gallery and are not referenced here:
`playground.png`, `audit-summary.png`, `landing.png`.
